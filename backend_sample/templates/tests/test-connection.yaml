apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "backend_sample.fullname" . }}-test-connection"
  labels:
    {{- include "backend_sample.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "backend_sample.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
